from setuptools import setup

microlib_name = 'samp20.asyncrunner'

setup(
    name=microlib_name,
    version="0.1.0",
    author="Sam Partridge",
    description="Runner for asyncio service components",
    packages=[microlib_name],
)